<?php
/**
 * Bracket page content.
 * Rendered by PTM_Public::handle_bracket_page() inside the active theme.
 * Variables available: $tournament (object), $bracket (array), $tournament_id (int)
 */
if ( ! defined( 'ABSPATH' ) ) exit;

// Reuse the shared bracket partial
require PTM_PLUGIN_DIR . 'public/views/bracket.php';
